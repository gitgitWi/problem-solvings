import { describe, test, expect } from "vitest";

describe("12985. 예상 대진표", () => {
  /**
   * @see {@link https://school.programmers.co.kr/learn/courses/30/lessons/12985}
   *
   * @param {number} n
   * @param {number} a
   * @param {number} b
   * @returns {number}
   */
  function solution(n, a, b) {
    let answer = 0;

    return answer;
  }

  test("TC1", () => {
    expect(solution(8, 4, 7)).toEqual(3);
  });
});
